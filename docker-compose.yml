version: "3"

services:

  document_service:
    build:
      context: .
      dockerfile: ./docker/Dockerfile
    container_name: document_service
    network_mode: host
    env_file:
      - ./.envs/.django
      - ./.envs/.postgres

#networks:
#  network1:
#    name: dr
#    external: true
